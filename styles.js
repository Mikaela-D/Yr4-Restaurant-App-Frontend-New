import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
  },
  productContainer: {
    marginBottom: 20,
    padding: 10,
    borderWidth: 1,
    borderColor: "#ccc",
    borderRadius: 5,
  },
  productText: {
    fontSize: 16,
  },
  addButton: {
    marginTop: 20,
  },
  errorText: {
    color: "red",
    marginBottom: 10,
  },
});

export default styles;
